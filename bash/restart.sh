#gnome commands
sudo {start|stop|restart} gdm


#shutdown / poweroff Ubuntu Linux

sudo halt
#or
sudo shutdown -h now

#reboot Ubuntu Linux

sudo reboot

#More information can be found about these two commands by typing following commands (man page):
man reboot
man shutdown
