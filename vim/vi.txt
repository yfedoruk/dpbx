#Selecting words while search:
:set hls 
#tabulation

#numbers
:set nu / set nonu

#normal paste from outer to vim
:set paste 
Иначе при вставке кода из буфера обмена он частенько вставляется «лесенкой».
